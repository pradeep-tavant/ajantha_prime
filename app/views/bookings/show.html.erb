<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3><%= @booking.reason %> - <%= @booking.approved? ? 'Approved' : 'Pending Approval' %>
            <% if current_member.admin? || (current_member == @booking.member && !@booking.approved?) %>
              <span class="pull-right">
                <%= link_to edit_booking_path(@booking) do %>
                  <i class="fa fa-pencil-square-o fa-fw" title="Edit"></i>
                <% end %>
              </span>
            <% end %>
          </h3>
        </div>
        <% if current_member.admin? %>
          <dl class="dl-horizontal mtop20">
            <dt>Name:</dt>
            <dd><%= link_to (@booking.member.try(:name)+'('+@booking.member.try(:login)+')'), member_path(@booking.member) %></dd>
          </dl>
        <% end %>
        <dl class="dl-horizontal mtop20">
          <dt>Reason:</dt>
          <dd><%= @booking.reason %></dd>
        </dl>
        <dl class="dl-horizontal mtop20">
          <dt>On Date:</dt>
          <dd>Rs.<%= @booking.on_date.strftime("%d/%b/%Y") %></dd>
        </dl>
        <dl class="dl-horizontal mtop20">
          <dt>Start Time:</dt>
          <dd><%= @booking.start_time.strftime("%l:%M %p") %></dd>
        </dl>
        <dl class="dl-horizontal mtop20">
          <dt>End Time:</dt>
          <dd><%= @booking.end_time.strftime("%l:%M %p") %></dd>
        </dl>
        <dl class="dl-horizontal mtop20">
          <dt>Notes:</dt>
          <dd><%= simple_format(@booking.notes) %></dd>
        </dl>
      </div>

      <%= link_to '<< Bookings', bookings_path, class: "btn btn-default" %>
    </div>
  </div>
</div>

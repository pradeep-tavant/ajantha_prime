<nav class="navbar navbar-default navbar-static-top" role="navigation" style="margin-bottom: 0">
  <div class="navbar-header">
    <%= link_to "APOWA", root_path, class: "navbar-brand navbar-link", title: "Ajantha Prime Owners Welfare Association" %>
  </div>
  <!-- /.navbar-header -->

  <% if current_member %>
    <div>
      <ul class = "nav navbar-nav">
         <li class = "<%= (controller_name=='members' && action_name=='index') ? '' : ''%>"><%= link_to "Members", members_path, class: "navbar-link" %></li>
      </ul>
    </div>
    <ul class="nav navbar-top-links navbar-right">
      <li class="dropdown">
        <a class="dropdown-toggle navbar-link" data-toggle="dropdown" href="#">
          Welcome <b><%=current_member.name%></b> <i class="fa fa-caret-down"></i>
        </a>
        <ul class="dropdown-menu dropdown-user">
          <li>
            <%= link_to member_path(current_member) do %>
              <i class="fa fa-user fa-fw"></i>
              Profile
            <% end %>
          </li>
          <li>
            <%= link_to change_password_path(current_member) do %>
              <i class="fa fa-key fa-fw"></i>
              Change Password
            <% end %>
          </li>
          <li class="divider"></li>
          <li>
            <%= link_to destroy_member_session_path, method: :delete do %>
              <i class="fa fa-sign-out fa-fw"></i>
              Logout
            <% end %>
          </li>
        </ul>
        <!-- /.dropdown-user -->
      </li>
      <!-- /.dropdown -->
    </ul>
    <!-- /.navbar-top-links -->
    <p class='fwhite pull-right mright20 mtop10'> <b>Login :</b> <%= current_member.login %> </p>
  <% end %>
</nav>
